@model IEnumerable<WeatherForecastASPNetCoreDBFirst.Models.HavaDurumuIcon>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Aciklama)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Aciklama)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IconId">Düzenle</a> |
                <a asp-action="Details" asp-route-id="@item.IconId">Detay</a> |
                <a asp-action="Delete" asp-route-id="@item.IconId">Sil</a>
            </td>
        </tr>
}
    </tbody>
</table>
